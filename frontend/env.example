# API Backend (proxy Core + Chat HTTP)
NEXT_PUBLIC_API_URL=http://localhost:3001
# Chat em tempo real (Socket.IO). Geralmente a URL do chat-service, ex: mesma porta do CHAT_SERVICE_URL do backend.
NEXT_PUBLIC_CHAT_WS_URL=http://localhost:9001
